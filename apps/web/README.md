# BOMBoard Web

This package contains the shared frontend application for BOMBoard.

Current status:

- workspace scaffold only
- no parsing or viewer features yet
- used by both the browser build and the Electron desktop shell
