{
  "name": "@bomboard/viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "pnpm --filter @bomboard/core build",
    "pretypecheck": "pnpm --filter @bomboard/core build",
    "build": "tsc -p tsconfig.json && cp src/package-rules.json dist/package-rules.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bomboard/core": "workspace:*",
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
